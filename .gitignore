# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Fresh build directory
_fresh/
# npm + other dependencies
node_modules/
vendor/

# Deno 锁文件(本仓选择忽略,避免锁文件 churn;Deno Deploy 会自行重新解析依赖)
deno.lock

